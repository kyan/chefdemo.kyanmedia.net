server {
  listen 80;

  server_name staging.chefdemo.kyanmedia.net;
  root /var/www/staging.chefdemo.kyanmedia.net/current/public;
  passenger_enabled on;
  rails_env staging;
  client_max_body_size 4M;

  # serve static content directly
  location ~* \.(ico|jpg|gif|png|swf|html)$ {
    if (-f $request_filename) {
      expires max;
      break;
    }
  }
}